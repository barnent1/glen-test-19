# Project: quetrex-app

## Verification
To verify the project is structurally sound and compiles correctly, run the following commands:

```bash
timeout 120 npm run type-check
timeout 300 npm run build
```
